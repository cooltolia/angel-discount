/*
var prBody = $(".pr-card__body");
var discount = $(".pr-card__discount");

for (var i = 0; i < prBody.length; i++) {
  if (prBody[i].clientWidth <= "200") {
    discount[i].style.padding = "6px 8px"
    console.log(prBody[i].clientWidth);
  }
}
*/
